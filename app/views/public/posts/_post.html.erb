<%= form_with(model: posts, url: posts_path, local: true) do |f| %>
  <%= f.hidden_field :genre, value: genre %>
  <%= f.text_field :body, placeholder:"new post" %><br>
  <%= f.file_field :image, accept: "image/*" %><br>
  <%= f.submit "投稿" %>
<% end %>